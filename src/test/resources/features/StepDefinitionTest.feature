@mock @pageObject=Mock
Feature: Step Definition Test

  Scenario: Test all step definitions
    And I click on the "mock"
    And I click on "mock"
    And I take the "mock" and enter it into the "mock"
    And I take "mock" and enter it into "mock"
    And I remember the text from the "mock" as "mock"
    And I remember the text from "mock" as "mock"
    * I enter a random email address into the "mock"
    * I enter a random phone number into the "mock"
    * I enter a random pokemon into the "mock"
    * I enter a random SSN into the "mock"
    * I enter a random title into the "mock"
    * I enter a random 10 digit phone number into the "mock"
